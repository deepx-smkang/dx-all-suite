# DX AI Studio has ZERO third-party runtime dependencies — the entire studio,
# including the dx_app ModelZoo tab (urllib + a stdlib html.parser DOM), runs on the
# Python standard library alone (Python 3.12+). Air-gapped boards install with a plain
# `pip install -e .` and no PyPI access. Test/CI extras live in requirements-ci.txt.
